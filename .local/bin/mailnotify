#!/bin/sh
unread=$(find "${XDG_DATA_HOME:-$HOME/.local/share}/mail" -path "*/[Ii][Nn][Bb][Oo][Xx]/new/*" -type f 2>/dev/null | wc -l)
[ "$unread" -gt 0 ] || exit 0
notify-send "📬 $unread neue Mail(s)" -i mail-message
