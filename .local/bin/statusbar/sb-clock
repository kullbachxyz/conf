#!/bin/sh


btn="${BLOCK_BUTTON:-$BUTTON}"
if [ -n "$btn" ]; then
	case "$btn" in
		1|2) setsid -f "$TERMINAL" -e calcurse ;;
		3) notify-send "🕒 Clock module" "Shows current date and time.
- Left click opens calcurse." ;;
	esac
fi

printf "🕒%s\n" "$(date "+%d %b %Y (%a) %H:%M")"
